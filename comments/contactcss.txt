/*
CSS Styling for Homepage and Contact Page (styles.css)

body {
    font-family: Arial, sans-serif;  - Sets the font for the entire document
    margin: 0;                       - Removes default margin
    padding: 0;                      - Removes default padding
    box-sizing: border-box;          - Ensures padding and border are included in element's total width and height
}

header {
    background-color: #4CAF50;       - Sets the background color of the header
    color: white;                    - Sets the text color to white
    padding: 10px 0;                 - Adds padding to the top and bottom of the header
}

nav ul {
    list-style-type: none;           - Removes default list styling
    padding: 0;                      - Removes default padding
    text-align: center;              - Centers the navigation links
}

nav ul li {
    display: inline;                 - Displays list items in a row
    margin: 0 15px;                  - Adds margin between navigation links
}

nav ul li a {
    color: white;                    - Sets the link text color to white
    text-decoration: none;           - Removes default underline from links
    font-weight: bold;               - Makes the link text bold
}

.contact {
    text-align: center;              - Centers the text
    padding: 50px 20px;              - Adds padding around the section
    background-color: #f4f4f4;       - Sets a light background color
}

.contact h1 {
    font-size: 2.5em;                - Sets the font size of the main heading
    margin-bottom: 20px;             - Adds margin below the heading
}

.contact p {
    font-size: 1.2em;                - Sets the font size of the description text
    color: #333;                     - Sets the text color
    margin-bottom: 40px;             - Adds margin below the description text
}

form {
    display: inline-block;           - Centers the form
    text-align: left;                - Aligns text to the left inside the form
    max-width: 600px;                - Sets a maximum width for the form
    width: 100%;                     - Ensures the form takes full width of its container
    background: white;               - Sets the background color of the form
    padding: 20px;                   - Adds padding inside the form
    border-radius: 8px;              - Rounds the corners of the form
    box-shadow: 0 0 10px rgba(0,0,0,0.1); - Adds a subtle shadow to the form
}

label {
    display: block;                  - Makes labels block elements
    font-weight: bold;               - Makes label text bold
    margin-bottom: 5px;              - Adds margin below labels
}

input[type="text"],
input[type="email"],
textarea {
    width: 100%;                     - Sets the width of input fields and textarea to 100%
    padding: 10px;                   - Adds padding inside input fields and textarea
    margin-bottom: 20px;             - Adds margin below input fields and textarea
    border: 1px solid #ccc;          - Sets the border of input fields and textarea
    border-radius: 4px;              - Rounds the corners of input fields and textarea
    font-size: 1em;                  - Sets the font size of input fields and textarea
}

textarea {
    resize: vertical;                - Allows vertical resizing of the textarea
}

button[type="submit"] {
    background-color: #4CAF50;       - Sets the background color of the submit button
    color: white;                    - Sets the text color of the submit button to white
    border: none;                    - Removes the border of the submit button
    padding: 15px 30px;              - Adds padding inside the submit button
    font-size: 1em;                  - Sets the font size of the submit button text
    border-radius: 4px;              - Rounds the corners of the submit button
    cursor: pointer;                 - Changes the cursor to a pointer when hovering over the button
}

button[type="submit"]:hover {
    background-color: #45a049;       - Changes the background color of the submit button when hovered
}

footer {
    background-color: #333;          - Sets the background color of the footer
    color: white;                    - Sets the text color to white
    text-align: center;              - Centers the text
    padding: 20px 0;                 - Adds padding around the footer
    position: relative;              - Sets the footer position
    bottom: 0;                       - Positions the footer at the bottom
    width: 100%;                     - Sets the footer width to fill the page
}

footer a {
    color: #4CAF50;                  - Sets the link color in the footer
    text-decoration: none;           - Removes default underline from links
}
*/
